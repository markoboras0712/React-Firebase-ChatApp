export enum Routes {
  Login = '/',
  Register = '/register',
  ForgotPassword = '/forgot-password',
  Messages = '/messages',
  Contacts = '/contacts',
  Message = '/messages/:id',
  NotFound = '*',
}
